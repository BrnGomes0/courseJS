    /*
                                                    FUNÇÕES

        Uma função JavaScript é um bloco de código projetado para executar uma tarefa específica.

        É como uma pequena "fábrica" onde você faz uma entrada e ele te dá uma saída.

        Pode ser encarado como "mini-programas" projetados para fazer uma tarefa que vai contribuir para todo código.

        Uma função JavaScript é executada quando "algo" a invoca (chama-a).

    */

    // // FUNÇÃO DE SOMA
    // function soma (valor1, valor2) {
    //     return valor1 + valor2; // Return da função. O que ela irá mandar de resposta
    // }
    // // FUNÇÃO DE COTAÇÃO DO DÓLAR
    // function realParaDolar(real, cotacaoDolar) {
    //     return real * cotacaoDolar
    // }

    // var valorReal = 7.89;
    // var cotacao = 5.08;

    // console.log(`O valor em Reais é de R$${valorReal}. Em dólar é $${realParaDolar(valorReal, cotacao).toFixed(2)}`);

// function alertHello() {
//     alert('Hello Guys!');
// } Chamando ela no HTML

// function paraCelsius (valorFahrenheit) {
//     return (5 / 9) * (valorFahrenheit - 32);
// }

// var x = paraCelsius(77);

// console.log(`A temperatura é de ${x} °C`)

function minhaFunao () {
    var x = 2; // Essa variável só funciona dentro dessa função (Desse Bloco)
}

// A variável x fora do bloco da função não funcionará. Irá dar um erro de Not Defined, pois ela não foi definada fora do bloco, apenas dentro da função

